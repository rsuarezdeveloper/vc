{% extends '::base.html.twig' %}

{%block page_header%}Crear Nueva Agencia{% endblock %}
{% block body -%}

    <form action="{{ path('agencia_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('agencia') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
